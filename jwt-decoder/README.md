# JWT Decoder

A free, client-side JWT (JSON Web Token) decoder. Inspect headers, payloads, and verify token structure — without sending your tokens to any server.

🔗 **[Try it live →](https://maxmini0214.github.io/jwt-decoder/)**

## Features

- ✅ Decode JWT header & payload instantly
- ✅ Expiration time check with human-readable dates
- ✅ Syntax-highlighted JSON output
- ✅ Supports all standard JWT algorithms
- ✅ 100% client-side — your tokens never leave your browser

## Usage

Paste a JWT token, get the decoded header and payload immediately. Color-coded expiration status tells you if the token is still valid.

---

## ⚡ Pro Version

Working with JWTs in production? **JWT Toolkit Pro** adds serious capabilities:

- 🔐 **Signature verification** — verify RS256, ES256, HS256 signatures with your keys
- 🏗️ **Token builder** — create and sign JWTs right in the browser
- ⏱️ **Expiration timeline** — visual timeline of token lifecycle (iat, nbf, exp)
- 🔗 **JWKS endpoint integration** — fetch and verify against remote key sets
- 📋 **Claim validation** — check issuer, audience, and custom claims against your rules
- 📊 **Batch decode** — decode multiple tokens at once for debugging auth flows

**[Get JWT Toolkit Pro →](https://maxmini.gumroad.com/l/jwt-toolkit-pro)**

*One-time purchase. No subscription. Free updates forever.*
